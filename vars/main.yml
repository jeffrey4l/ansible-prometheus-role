---
go_arch_map:
  i386: '386'
  x86_64: 'amd64'
  aarch64: 'arm64'
  armv7l: 'armv7'
  armv6l: 'armv6'

prometheus_checksums:
  2.4.2: "{{ lookup('file', role_path ~ '/vars/2.4.2.sha256sums.yml')|from_yaml }}"
  2.4.3: "{{ lookup('file', role_path ~ '/vars/2.4.3.sha256sums.yml')|from_yaml }}"
