---
go_arch_map:
  i386: '386'
  x86_64: 'amd64'
  aarch64: 'arm64'
  armv7l: 'armv7'
  armv6l: 'armv6'

prometheus_checksums:
  2.5.0: "{{ lookup('file', role_path ~ '/vars/versions/2.5.0.sha256sums.yml')|from_yaml }}"
  2.4.3: "{{ lookup('file', role_path ~ '/vars/versions/2.4.3.sha256sums.yml')|from_yaml }}"
  2.4.2: "{{ lookup('file', role_path ~ '/vars/versions/2.4.2.sha256sums.yml')|from_yaml }}"
  2.4.1: "{{ lookup('file', role_path ~ '/vars/versions/2.4.1.sha256sums.yml')|from_yaml }}"
  2.4.0: "{{ lookup('file', role_path ~ '/vars/versions/2.4.0.sha256sums.yml')|from_yaml }}"
  2.3.2: "{{ lookup('file', role_path ~ '/vars/versions/2.3.2.sha256sums.yml')|from_yaml }}"
  2.3.1: "{{ lookup('file', role_path ~ '/vars/versions/2.3.1.sha256sums.yml')|from_yaml }}"
  2.3.0: "{{ lookup('file', role_path ~ '/vars/versions/2.3.0.sha256sums.yml')|from_yaml }}"
  2.2.1: "{{ lookup('file', role_path ~ '/vars/versions/2.2.1.sha256sums.yml')|from_yaml }}"
  2.2.0: "{{ lookup('file', role_path ~ '/vars/versions/2.2.0.sha256sums.yml')|from_yaml }}"
  2.1.0: "{{ lookup('file', role_path ~ '/vars/versions/2.1.0.sha256sums.yml')|from_yaml }}"
  2.0.0: "{{ lookup('file', role_path ~ '/vars/versions/2.0.0.sha256sums.yml')|from_yaml }}"
  1.8.2: "{{ lookup('file', role_path ~ '/vars/versions/1.8.2.sha256sums.yml')|from_yaml }}"
  1.8.1: "{{ lookup('file', role_path ~ '/vars/versions/1.8.1.sha256sums.yml')|from_yaml }}"
  1.8.0: "{{ lookup('file', role_path ~ '/vars/versions/1.8.0.sha256sums.yml')|from_yaml }}"
  1.7.2: "{{ lookup('file', role_path ~ '/vars/versions/1.7.2.sha256sums.yml')|from_yaml }}"
  1.7.1: "{{ lookup('file', role_path ~ '/vars/versions/1.7.1.sha256sums.yml')|from_yaml }}"
  1.7.0: "{{ lookup('file', role_path ~ '/vars/versions/1.7.0.sha256sums.yml')|from_yaml }}"
  1.6.3: "{{ lookup('file', role_path ~ '/vars/versions/1.6.3.sha256sums.yml')|from_yaml }}"
  1.6.1: "{{ lookup('file', role_path ~ '/vars/versions/1.6.1.sha256sums.yml')|from_yaml }}"
  1.6.0: "{{ lookup('file', role_path ~ '/vars/versions/1.6.0.sha256sums.yml')|from_yaml }}"
